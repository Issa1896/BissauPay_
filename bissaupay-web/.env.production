# Deploy Vercel — frontend e backend no mesmo domínio
# O VITE_API_URL é injetado via variável de ambiente na Vercel.
# Se não definido, o cliente usa '/api/v1' (default em src/services/api.js)
# VITE_API_URL=https://bissaupay.vercel.app/api/v1
